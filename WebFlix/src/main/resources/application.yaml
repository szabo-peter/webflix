spring:
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/webflix
    password: admin
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update